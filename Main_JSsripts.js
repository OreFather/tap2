var buttonCount = 0;
function selection(a, b) {
    buttonCount = buttonCount + 1;
    
    if (buttonCount == 2) {
        dataSearch(a, b)
    } else {
        
    }
        
}
function dataSearch(l, m) {
    
}